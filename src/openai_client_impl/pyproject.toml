[project]
name = "openai-client-impl"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "openai>=1.40.0",
  "sqlalchemy>=2",
  "cryptography",
  "python-dotenv",
  "itsdangerous",
]
packages = [{ include = "openai_client_impl", from = "src" }]
